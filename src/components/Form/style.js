import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  formControl: {
    marginBottom: 50,
    minWidth: 120,
    width: "100%",
  },
  sol: {
    width: "100%",
    marginBottom: 50,
  },
}));
